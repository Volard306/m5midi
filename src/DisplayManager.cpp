#include "DisplayManager.h"

DisplayManager::DisplayManager(EventQueue& q) : queue(q) {}

void DisplayManager::begin() {
  // init OLED later
}

void DisplayManager::update() {
  // stub display update
}
